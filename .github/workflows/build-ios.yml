name: Build Nowhere iOS

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-ios:
    name: Build & Package iOS App
    runs-on: macos-14

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Select Xcode Version
        run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer

      - name: Build iOS App for Generic iOS Device
        run: |
          cd ios
          xcodebuild clean build \
            -project Nowhere.xcodeproj \
            -scheme Nowhere \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            PROVISIONING_PROFILE="" \
            SYMROOT=build

      - name: Package .app into Zip
        run: |
          cd ios/build/Release-iphoneos
          zip -r Nowhere-iOS-Release.zip Nowhere.app

      - name: Upload iOS App Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nowhere-iOS-App
          path: ios/build/Release-iphoneos/Nowhere-iOS-Release.zip
          if-no-files-found: error
          retention-days: 14
